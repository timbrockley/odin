package add

import "core:testing"

//------------------------------------------------------------

@(test)
test_add :: proc(t: ^testing.T) {
	//----------------------------------------
	result := add(2, 3)
	testing.expect(t, result == 5, "add(2, 3) should be 5")
	//----------------------------------------
}

//------------------------------------------------------------
